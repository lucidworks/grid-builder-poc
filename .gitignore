# macOS
.DS_Store

# Editor directories
.vscode/
.idea/

# Node modules (if ever added)
node_modules/

# Build outputs from stencil-src - MUST be committed for GitHub Pages deployment
# (GitHub Pages serves files directly without running a build step)
