# macOS
.DS_Store

# Editor directories
.vscode/
.idea/

# Node modules (if ever added)
node_modules/

# Build outputs from stencil-src (build/ and loader/ only - index.html and shared/ are deployed to GitHub Pages)
stencil/build/
stencil/loader/
